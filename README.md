# API endpoint (test task)

For setup project use:
* create new virtual environment using python 3.9
* run 'pip install -r requirements.txt' 

For run tests:
* run 'python -m unittest discover tests'
